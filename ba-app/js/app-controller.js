/**
 * This is the main controller for the app and does all the real work of switching screens, etc.
 *

 */

app.controller("baAppController", function($scope){


    // Should start with a clean slate each user session
    $scope.session = {};


});
